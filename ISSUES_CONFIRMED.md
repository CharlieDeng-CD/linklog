# 问题确认报告

## 问题 1: 子节点展开缓存机制缺失 ✅ 已确认

### 问题描述
子节点展开后收起，再次展开时需要重新调用 API，内容可能不一样，而且不是马上展开。

### 问题确认

**前端代码分析 (`GraphCanvas.tsx`):**

1. **展开逻辑** (第162行):
```typescript
if (!node.data.expanded && !expandingNodeId) {
  // 调用 API 展开节点
}
```

2. **收起逻辑** (第89-145行):
```typescript
const handleNodeCollapse = useCallback((nodeId: string) => {
  // 删除子节点和边
  // 设置 expanded: false
  const updatedNodes = allNodes.map((n) =>
    n.id === nodeId ? { ...n, data: { ...n.data, expanded: false } } : n
  );
  // 删除 nodeChildren 记录
  newNodeChildren.delete(nodeId);
}, []);
```

3. **问题根源**:
   - ✅ 收起时，`expanded` 被设置为 `false`
   - ✅ 子节点和边被删除
   - ✅ `nodeChildren` 记录被删除
   - ❌ **没有缓存机制**：再次展开时，由于 `expanded` 是 `false`，会重新调用 API
   - ❌ **没有保存子节点数据**：虽然有 `nodeChildren` 记录子节点 ID，但没有保存子节点的完整数据

### 影响
- 每次展开都需要重新调用 API，浪费时间和 Token
- AI 生成的内容可能不同，导致用户体验不一致
- 响应时间较长，不是"马上展开"

---

## 问题 2: 孤立节点问题 ✅ 已确认

### 问题描述
生成的若干节点存在孤立节点，希望修复让各个节点都能关联起来。

### 问题确认

**后端代码分析 (`server.py`):**

1. **节点生成逻辑** (第1238-1276行):
   - AI 生成节点和边
   - 执行节点去重
   - **但没有检查孤立节点**

2. **问题根源**:
   - ❌ AI 可能生成没有边的节点
   - ❌ 没有验证所有节点是否都有边连接
   - ❌ 没有自动创建边来连接孤立节点

### 影响
- 图谱中存在孤立节点，无法形成完整的知识依赖关系
- 用户体验不佳，节点之间没有关联

---

## 修复方案

### 问题 1 修复方案：添加缓存机制

1. **保存子节点数据**：
   - 创建一个 `expandedNodesCache` Map，保存每个节点展开后的完整数据（节点和边）
   - 当节点展开时，将结果保存到缓存中
   - 当节点收起时，保留缓存数据，只从视图中移除

2. **使用缓存数据**：
   - 当节点再次展开时，先检查缓存
   - 如果缓存存在，直接使用缓存数据，立即展开（无需等待 API）
   - 如果缓存不存在，才调用 API

3. **实现位置**：
   - 前端：`frontend/components/GraphCanvas.tsx`

### 问题 2 修复方案：确保节点连接

1. **检测孤立节点**：
   - 在生成节点后，检查哪些节点没有边连接
   - 识别孤立节点

2. **自动创建边**：
   - 对于孤立节点，自动创建边连接到最近的节点
   - 或者连接到根节点（原始目标）
   - 或者连接到同一层级的其他节点

3. **实现位置**：
   - 后端：`server.py` 的 `v2_init_graph` 和 `v2_expand_node` 函数

---

## 修复优先级

1. **高优先级**：问题 1（缓存机制）- 影响用户体验和性能
2. **高优先级**：问题 2（孤立节点）- 影响图谱完整性

两个问题都需要立即修复。

