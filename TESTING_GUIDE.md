# 测试指南

## 测试准备

### 服务状态
- ✅ 后端服务：运行在 `http://localhost:8003`
- ✅ 前端服务：运行在 `http://localhost:3000`

### 已修复的功能

1. **子节点展开缓存机制** ✅
   - 展开节点后，数据会缓存
   - 收起后再次展开，会立即显示（无需等待 API）
   - 内容保持一致

2. **孤立节点处理** ✅
   - 改进了 Prompt，要求 AI 为所有节点创建边
   - 检测到孤立节点会记录警告，但不强行连接
   - 保持图谱的逻辑完整性

## 测试步骤

### 测试 1: 缓存机制

1. **打开前端页面** (`http://localhost:3000`)
2. **生成初始图谱**：
   - 输入目标（如："开发个人博客"）
   - 点击生成
   - 等待初始图谱生成

3. **展开节点**：
   - 点击一个节点（如："了解Git版本控制"）
   - 等待子节点生成（第一次会调用 API）

4. **收起节点**：
   - 双击该节点
   - 子节点应该被收起

5. **再次展开节点**：
   - 再次点击该节点
   - ✅ **预期结果**：子节点应该**立即显示**（无需等待 API）
   - ✅ **预期结果**：内容应该**保持一致**

### 测试 2: 孤立节点检测

1. **生成初始图谱**：
   - 输入目标
   - 观察日志输出

2. **检查日志**：
   - 查看后端日志 (`server.log`)
   - 查找 "检查节点连接情况" 的日志
   - ✅ **预期结果**：如果所有节点都有边，应该显示 "✅ 所有节点都已连接"
   - ⚠️ **如果发现孤立节点**：会显示警告，但不会强行连接

3. **展开节点**：
   - 展开一个节点
   - 检查子节点是否都连接到父节点
   - ✅ **预期结果**：所有子节点都应该有边连接到父节点

### 测试 3: 图谱完整性

1. **检查节点连接**：
   - 查看图谱中的所有节点
   - ✅ **预期结果**：所有节点都应该有边连接（基于知识逻辑）

2. **检查边的合理性**：
   - 查看边的 reason 字段
   - ✅ **预期结果**：边的 reason 应该清晰说明连接的原因

## 预期结果

### 缓存机制测试
- ✅ 第一次展开：需要等待 API（5-10秒）
- ✅ 收起后再次展开：**立即显示**（<1秒）
- ✅ 内容保持一致

### 孤立节点测试
- ✅ Prompt 改进后，AI 应该为所有节点创建边
- ✅ 如果仍有孤立节点，会记录警告但不强行连接
- ✅ 图谱的逻辑关系清晰

## 问题排查

### 如果缓存不工作
1. 检查浏览器控制台是否有错误
2. 检查后端日志是否有错误
3. 确认前端代码已更新

### 如果仍有孤立节点
1. 查看后端日志中的警告信息
2. 检查 Prompt 是否已更新
3. 考虑进一步改进 Prompt

## 日志位置

- **后端日志**：`/Users/mugezi/Documents/projects/linklog/server.log`
- **前端日志**：浏览器控制台（F12）

## 测试检查清单

- [ ] 后端服务正常运行
- [ ] 前端服务正常运行
- [ ] 可以生成初始图谱
- [ ] 可以展开节点
- [ ] 可以收起节点
- [ ] 再次展开时立即显示（缓存工作）
- [ ] 内容保持一致
- [ ] 所有节点都有边连接
- [ ] 边的 reason 合理


